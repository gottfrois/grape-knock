require 'spec_helper'

describe Grape::Knock do
  it 'has a version number' do
    expect(Grape::Knock::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
